# Frozen pathology encoder (TITAN target; ResNet50 ImageNet fallback in code today)
encoder_id: TITAN
encoder_version: pending_access
fallback_encoder_id: resnet50_imagenet
freeze: true
embedding_dim: 2048
